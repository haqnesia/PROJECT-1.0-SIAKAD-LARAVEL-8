
# AKTIFKAN MENU

    href="{{ route('fakultas.index') }}"

# BAGIAN 2


    # ROUTE MASTER FAKULTAS

    Route::middleware(['auth'])->prefix('fakultas')->as('fakultas.')->group(function () {

        Route::get('/', [MasterFakultasController::class, 'index'])->name('index');

    });


# BAGIAN 3

  use App\Http\Controllers\MasterFakultasController;

# BAGIAN 4 (CONTROLLER)

    php artisan make:controller MasterFakultasController

    # FUNGSI INDEX

    public function index()
    {
        return view('master_fakultas.index');
    }

# BAGIAN 5 (VIEW)


    Buat Folder  master_fakultas

    copy home.blade.php ke master_fakultas, rename menjadi index.blade.php

    Copy isi content.blade.php

# FINALLY (CARA YANG SAMA)

    href="{{ route('fakultas.index') }}"
    href="{{ route('prodi.index') }}"
    href="{{ route('mahasiswa.index') }}"

    # ROUTE MASTER PRODI

    Route::middleware(['auth'])->prefix('prodi')->as('prodi.')->group(function () {

        Route::get('/', [MasterProdiController::class, 'index'])->name('index');

    });

    # ROUTE MASTER MAHASISWA

    Route::middleware(['auth'])->prefix('mahasiswa')->as('mahasiswa.')->group(function () {

        Route::get('/', [MasterMahasiswaController::class, 'index'])->name('index');

    });


    use App\Http\Controllers\MasterProdiController;
    use App\Http\Controllers\MasterMahasiswaController;

    php artisan make:controller MasterProdiController
    php artisan make:controller MasterMahasiswaController


    Buat Function Index pada Prodi dan Mahasiswa


    master_prodi
    master_mahasiswa


    Rename Header




